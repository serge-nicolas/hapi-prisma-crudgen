hapi:
  routes:
    api:
      - path: /api/upload
        method: post

      - path: /api/graphql
        method: post

    cors:
      origin:
        - other_host:4000
      headers:
        - Accept
        - Content-Type
      additionalHeaders:
        - X-Requested-With